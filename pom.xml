<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.jenkins-ci.plugins</groupId>
    <artifactId>plugin</artifactId>
    <version>1.424.6</version><!-- which version of Jenkins is this plugin built against? -->
  </parent>

  <groupId>com.unitedinternet.jenkins.plugins.jobtemplates</groupId>
  <artifactId>jobtemplates</artifactId>
  <version>1.0-SNAPSHOT</version>
  <packaging>hpi</packaging>
 	<description>Create Jobs by copying existing templates</description>
	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
		<maven-surefire-plugin.version>2.12</maven-surefire-plugin.version>
    <maven-surefire-report-plugin.version>${maven-surefire-plugin.version}</maven-surefire-report-plugin.version>
	</properties>
	<developers>
 	  <developer>
			<id>kstutz</id>
			<name>Kathi Stutz</name>
			<email>kathi.stutz@1und1.de</email>
    </developer>
  </developers>
	<scm>
    <connection>scm:git:ssh://github.com/kstutz/jobtemplates.git</connection>
    <developerConnection>scm:git:ssh://git@github.com/kstutz/jobtemplates.git</developerConnection>
    <url>https://github.com/kstutz/jobtemplates</url>
  </scm>

  <!-- get every artifact through repo.jenkins-ci.org, which proxies all the artifacts that we need -->
  <repositories>
    <repository>
      <id>repo.jenkins-ci.org</id>
      <url>http://repo.jenkins-ci.org/public/</url>
    </repository>
  </repositories>

  <pluginRepositories>
    <pluginRepository>
      <id>repo.jenkins-ci.org</id>
      <url>http://repo.jenkins-ci.org/public/</url>
    </pluginRepository>
  </pluginRepositories>
</project>
